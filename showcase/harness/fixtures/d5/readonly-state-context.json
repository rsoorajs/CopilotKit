{
  "_comment": "D5 fixture for /demos/readonly-state-agent-context. Asserts the agent received and used a value from read-only context. Substring 'recall the user preference' is unique.",
  "fixtures": [
    {
      "match": { "userMessage": "recall the user preference" },
      "response": {
        "content": "Per the read-only context the user prefers concise responses. The agent received this preference via the shared-state context and is honoring it without writing back to state."
      }
    }
  ]
}
